package com.project.gimme.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import com.project.gimme.pojo.GroupNotice;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.internal.DaoConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.

/**
 * DAO for table "group_notice".
 */
public class GroupNoticeDao extends AbstractDao<GroupNotice, Integer> {

    public static final String TABLENAME = "group_notice";

    /**
     * Properties of entity GroupNotice.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Integer.class, "id", true, "id");
        public final static Property OwnerId = new Property(1, Integer.class, "ownerId", false, "owner_id");
        public final static Property GroupId = new Property(2, Integer.class, "groupId", false, "group_id");
        public final static Property Text = new Property(3, String.class, "text", false, "text");
    }


    public GroupNoticeDao(DaoConfig config) {
        super(config);
    }

    public GroupNoticeDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /**
     * Creates the underlying database table.
     */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists ? "IF NOT EXISTS " : "";
        db.execSQL("CREATE TABLE " + constraint + "\"group_notice\" (" + //
                "\"id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"owner_id\" INTEGER," + // 1: ownerId
                "\"group_id\" INTEGER," + // 2: groupId
                "\"text\" TEXT);"); // 3: text
    }

    /**
     * Drops the underlying database table.
     */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"group_notice\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, GroupNotice entity) {
        stmt.clearBindings();

        Integer id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }

        Integer ownerId = entity.getOwnerId();
        if (ownerId != null) {
            stmt.bindLong(2, ownerId);
        }

        Integer groupId = entity.getGroupId();
        if (groupId != null) {
            stmt.bindLong(3, groupId);
        }

        String text = entity.getText();
        if (text != null) {
            stmt.bindString(4, text);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, GroupNotice entity) {
        stmt.clearBindings();

        Integer id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }

        Integer ownerId = entity.getOwnerId();
        if (ownerId != null) {
            stmt.bindLong(2, ownerId);
        }

        Integer groupId = entity.getGroupId();
        if (groupId != null) {
            stmt.bindLong(3, groupId);
        }

        String text = entity.getText();
        if (text != null) {
            stmt.bindString(4, text);
        }
    }

    @Override
    public Integer readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getInt(offset + 0);
    }

    @Override
    public GroupNotice readEntity(Cursor cursor, int offset) {
        GroupNotice entity = new GroupNotice( //
                cursor.isNull(offset + 0) ? null : cursor.getInt(offset + 0), // id
                cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // ownerId
                cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // groupId
                cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // text
        );
        return entity;
    }

    @Override
    public void readEntity(Cursor cursor, GroupNotice entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getInt(offset + 0));
        entity.setOwnerId(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setGroupId(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setText(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
    }

    @Override
    protected final Integer updateKeyAfterInsert(GroupNotice entity, long rowId) {
        return entity.getId();
    }

    @Override
    public Integer getKey(GroupNotice entity) {
        if (entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(GroupNotice entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }

}
